/**
 * 内置知识模块（Markdown 格式，随插件发布）。
 * 用户不可删除，但可禁用。首次启用插件时自动索引。
 */

export const BUILTIN_MODULES = [
  {
    id: 'builtin-modbus-rtu',
    name: 'Modbus RTU 协议手册',
    description: 'Modbus RTU 常用功能码、帧格式、CRC 计算与常见问题。',
    version: '1.0.0',
    tags: ['modbus', 'rtu', '协议'],
    docs: [
      {
        title: '功能码速查',
        content: `# 功能码速查

## 常用功能码

| 功能码 | 名称 | 说明 |
| ------ | ---- | ---- |
| 01 | 读线圈 | 读取离散输出 ON/OFF |
| 02 | 读离散输入 | 读取离散输入状态 |
| 03 | 读保持寄存器 | 读取 16 位寄存器 |
| 04 | 读输入寄存器 | 读取只读寄存器 |
| 05 | 写单线圈 | 写单个离散输出 |
| 06 | 写单寄存器 | 写单个保持寄存器 |
| 15 | 写多线圈 | 写多个离散输出 |
| 16 | 写多寄存器 | 写多个保持寄存器 |

## 选型建议

- 读取开关量用 01/02，读取模拟量或参数用 03/04。
- 写单个点用 05/06，批量写用 15/16，批量写能减少通信次数。`,
      },
      {
        title: '帧格式与字节序',
        content: `# 帧格式与字节序

## RTU 帧结构

从站地址(1B) + 功能码(1B) + 数据(N B) + CRC16(2B)。

帧与帧之间以 3.5 个字符时间的静默间隔分隔（波特率 9600 时约 4ms）。

## 字节序

- 寄存器值默认大端：高字节在前、低字节在后。
- 32 位数据（如浮点、32 位整数）常见两种排列：ABCD（大端）与 CDAB（字交换），需按设备手册确认。

## 调试技巧

抓取原始十六进制帧后，按「地址 + 功能码 + 数据 + CRC」逐段核对，最容易出错的是 CRC 字节序与寄存器地址偏移（0 基址还是 1 基址）。`,
      },
      {
        title: 'CRC16 校验计算',
        content: `# CRC16 校验计算

Modbus 使用 CRC-16/MODBUS 多项式 0xA001（反转多项式）。

## 计算步骤

1. 初始值 0xFFFF。
2. 对每个字节：与 CRC 低字节异或，右移 8 次，每次判断最低位是否为 1，为 1 则与 0xA001 异或。
3. 最终 CRC 按「低字节在前、高字节在后」追加到帧尾。

## 校验方法

用已知帧核对：地址 01、功能码 03、数据 00 00 00 01，其 CRC 为 84 0A（低字节 0A 在前）。

若设备返回 CRC 错误，优先检查 CRC 字节序是否写反、是否覆盖了地址到数据之间的全部字节。`,
      },
      {
        title: '寄存器与线圈地址',
        content: `# 寄存器与线圈地址

## 地址范围

- 线圈/离散输入：地址从 0 开始（PLC 中常写作 00001~09999，协议里减 1）。
- 保持/输入寄存器：地址从 0 开始（PLC 中常写作 40001，协议里为 0）。

## 常见陷阱

协议地址是「0 基址」，而很多文档写「1 基址」或带前缀（4xxxx），发送前要确认是否减 1。

例如读保持寄存器 40001，实际发送的起始地址是 0x0000；读 40010，实际发送 0x0009。`,
      },
      {
        title: '常见问题与排错',
        content: `# 常见问题与排错

## 无响应

- 检查从站地址是否正确、RS485 方向控制（DE/RE）是否及时切换。
- 检查总线是否接偏置电阻与终端电阻（120Ω）。

## 返回异常码

- 01 非法功能码、02 非法数据地址、03 非法数据值。
- 返回异常码时功能码最高位置 1（如 03 变为 83）。

## CRC 错误

多为波特率不准、接线过长、地电位差导致，检查波特率容差与屏蔽接地。`,
      },
    ],
  },
  {
    id: 'builtin-at-commands',
    name: 'AT 指令速查',
    description: '通用 AT 指令、常见无线模块指令与错误码说明。',
    version: '1.0.0',
    tags: ['at', '模块', '指令'],
    docs: [
      {
        title: '通用 AT 指令',
        content: `# 通用 AT 指令

## 基础指令

- AT：测试通信，返回 OK。
- ATE0 / ATE1：关闭 / 开启回显。
- AT&F：恢复出厂设置。
- ATZ：软复位。

## 查询与设置

- AT+CMD?：查询当前值。
- AT+CMD=?：查询取值范围。
- AT+CMD=<值>：设置参数。

## 注意事项

AT 指令以 \\r\\n 结尾，多数模块要求指令间留出足够响应时间；先发 AT 确认波特率与接线正常，再发具体指令。`,
      },
      {
        title: '常见模块指令（WiFi/4G）',
        content: `# 常见模块指令（WiFi/4G）

## ESP8266 / ESP32

- AT+CWMODE=1：设为 Station 模式。
- AT+CWJAP="ssid","pwd"：连接 WiFi。
- AT+CIPSTART="TCP","ip",port：建立 TCP 连接。
- AT+CIPSEND=n：发送 n 字节数据。

## EC20 / 4G 模块

- AT+CREG?：查询网络注册状态。
- AT+CSQ：查询信号强度。
- AT+CGATT=1：附着网络。

连接类指令要先确认模块已注册网络（CREG=1）再发起 TCP/UDP。`,
      },
      {
        title: '错误码说明',
        content: `# 错误码说明

## 常见返回值

- OK：成功。
- ERROR：指令格式或参数错误。
- +CME ERROR: <err>：移动设备相关错误。
- +CMS ERROR: <err>：短信相关错误。

## CME 错误码

- 10：SIM 卡未插入。
- 11：需要 PIN 码。
- 50：请求的功能不支持。

出现 CME ERROR 先检查 SIM 卡、网络注册与模块固件版本。`,
      },
      {
        title: '波特率与流控配置',
        content: `# 波特率与流控配置

## 波特率

- AT+IPR：查询 / 设置固定波特率。
- AT+IPR=115200：设为 115200。
- AT+IPR=0：恢复自适应波特率。

## 流控

AT+IFC 设置硬件流控（RTS/CTS）；数据量大时建议开启硬件流控避免丢数据。

修改波特率后模块会以新波特率响应，调试工具也要同步切换。`,
      },
      {
        title: '网络与 MQTT 相关指令',
        content: `# 网络与 MQTT 相关指令

## 网络

- AT+CIFSR：查询本机 IP。
- AT+PING="host"：ping 测试。
- AT+CIPSTATUS：查询连接状态。

## MQTT（以 ESP 系列为例）

- AT+MQTTUSERCFG：配置客户端 ID、用户名、密码。
- AT+MQTTCONN：连接 Broker。
- AT+MQTTPUB：发布消息。
- AT+MQTTSUB：订阅主题。

先确认 TCP 层可达（ping 通），再排查 MQTT 握手与鉴权。`,
      },
    ],
  },
  {
    id: 'builtin-serial-troubleshooting',
    name: '串口调试常见问题',
    description: '串口不通、乱码、丢数据等常见问题排查指南。',
    version: '1.0.0',
    tags: ['串口', '调试', '排错'],
    docs: [
      {
        title: '串口不通排查',
        content: `# 串口不通排查

## 排查顺序

1. 确认设备枚举：系统设备管理器 / dmesg 是否出现对应串口。
2. 确认占用：是否有其他程序占用了该串口。
3. 确认接线：TXD 接对方 RXD、RXD 接对方 TXD、GND 共地。
4. 确认参数：波特率、数据位、停止位、校验位是否一致。

## 回环测试

短接 TXD 与 RXD，自发自收验证本机串口与驱动是否正常。`,
      },
      {
        title: '乱码问题排查',
        content: `# 乱码问题排查

## 常见原因

- 波特率不匹配：最常见的乱码原因。
- 数据位 / 停止位不一致。
- 校验位不一致。
- 电平不匹配（TTL 3.3V 接 5V 或 RS232 接 TTL）。

## 定位方法

固定发送已知字节（如 0x55），用示波器或逻辑分析仪测量位宽，反推实际波特率。

若偶尔乱码，检查供电纹波与共地是否良好。`,
      },
      {
        title: '丢数据与溢出',
        content: `# 丢数据与溢出

## 原因

- 接收缓冲不足，读线程不及时导致溢出。
- 未开启硬件流控，高速大数据量时接收端来不及处理。
- 操作系统调度延迟。

## 对策

- 增大接收缓冲与串口 FIFO。
- 开启 RTS/CTS 硬件流控。
- 降低波特率或增加发送端节流。

调试时观察是否有「帧不完整」现象，通常意味着丢字节而非协议错误。`,
      },
      {
        title: '波特率与时钟偏差',
        content: `# 波特率与时钟偏差

## 容差

串口允许的波特率偏差一般不超过 ±2%~3%。晶振偏差、温度漂移会导致时钟偏差累积。

## 影响

偏差过大会导致起始位错位、采样点漂移，表现为周期性乱码或丢字节。

## 对策

- 使用高精度晶振。
- 主从双方波特率尽量精确匹配。
- 高速率（115200 以上）对时钟精度要求更高。`,
      },
      {
        title: '电平与接线问题',
        content: `# 电平与接线问题

## 电平标准

- TTL：0~3.3V 或 0~5V，常见于单片机与模块。
- RS232：±3V~±15V，负逻辑。
- RS485：差分信号，需 A/B 两根线。

## 常见错误

- TTL 直连 RS232 电平不匹配，需要 MAX232 类转换芯片。
- RS485 未接共地与终端电阻，长距离通信不稳定。
- 3.3V 与 5V 器件直连，可能损坏低压器件。`,
      },
    ],
  },
];
